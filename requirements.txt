# Bounds are intentionally loose: dllm pins transformers==4.57.0, and
# tightening here only causes resolution conflicts.
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.30.0
datasets>=2.18.0
tokenizers>=0.15.0
huggingface_hub>=0.23.0
safetensors>=0.4.0

# dllm is installed by scripts/setup_dllm.sh (upstream packaging bug means
# `pip install git+...` ships only the top-level package).

numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
openpyxl>=3.1.0
scikit-learn>=1.3.0
einops>=0.7.0

matplotlib>=3.7.0
seaborn>=0.13.0

tqdm>=4.65.0
typer>=0.12.0
wandb>=0.16.0

# Dev
ruff>=0.4.0
pytest>=8.0.0
pre-commit>=3.7.0
